<h1>Users</h1>

<% @users.each do |user| %>
	<p>
		<%= user.id %>:<%= user.username %>
		<%= link_to "[diary]",diaries_path(user.id)  %>
		<%= link_to "[edit]", edit_user_path(user.id)%>
	</p>
<% end %>

<%= link_to "[new user]", users_new_path  %>
